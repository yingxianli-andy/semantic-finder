# 项目缺失项清单

**检查日期**: 2024-01-09  
**项目位置**: `/root/autodl-tmp/ijcai`

---

## ❌ 必需项（必须安装才能运行）

### 1. 依赖包

#### ❌ torch-geometric
**状态**: 未安装  
**影响**: Agent 模块无法导入，训练无法运行  
**安装命令**:
```bash
pip install torch-geometric
```

#### ❌ gymnasium
**状态**: 未安装  
**影响**: Environment 模块无法导入，环境无法使用  
**安装命令**:
```bash
pip install gymnasium
```

#### ❌ scipy
**状态**: 未安装  
**影响**: 观点动力学模型无法运行（使用稀疏矩阵）  
**安装命令**:
```bash
pip install scipy
```

---

## ⚠️ 可选项（可选，但建议安装）

### 2. LLM 相关（测试时需要）

#### ⚠️ transformers
**状态**: 未安装  
**影响**: LLM 功能无法使用（测试时需要）  
**说明**: 训练时不需要，测试时如果需要使用 LLM 则需要  
**安装命令**:
```bash
pip install transformers accelerate
```

---

## ✅ 已安装的依赖

- ✅ torch (2.8.0+cu128) - 已安装
- ✅ networkx - 已安装
- ✅ numpy - 已安装（通常随 torch 安装）

---

## 🚀 快速安装（一键安装所有必需包）

```bash
cd /root/autodl-tmp/ijcai

# 方法 1: 使用 requirements.txt（推荐）
pip install -r requirements.txt

# 方法 2: 使用国内镜像（如果网络慢）
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# 方法 3: 手动安装必需包
pip install torch-geometric gymnasium scipy transformers accelerate tqdm pyyaml
```

---

## 📋 安装后验证

```bash
# 测试核心模块导入
python3 << EOF
import sys
sys.path.insert(0, '.')

# 测试 Agent
try:
    from src.agent import SemanticFINDER
    print('✓ Agent 模块正常')
except Exception as e:
    print(f'✗ Agent 模块失败: {e}')

# 测试 Environment
try:
    from src.environment import OpinionDynamicsEnv
    print('✓ Environment 模块正常')
except Exception as e:
    print(f'✗ Environment 模块失败: {e}')

# 测试 LLM（可选）
try:
    from src.llm import LLMController
    print('✓ LLM 模块正常')
except Exception as e:
    print(f'⚠ LLM 模块失败（可选）: {e}')
EOF
```

---

## 🎯 最小运行要求总结

### 训练阶段（必需）
1. ✅ torch (已有)
2. ❌ **torch-geometric** (需要安装)
3. ❌ **gymnasium** (需要安装)
4. ❌ **scipy** (需要安装)
5. ✅ networkx (已有)
6. ✅ numpy (已有)

### 测试阶段（额外需要）
7. ⚠️ transformers (可选，如果使用 LLM)

---

## 📝 完整安装步骤

```bash
# 1. 进入项目目录
cd /root/autodl-tmp/ijcai

# 2. 创建虚拟环境（推荐）
python3 -m venv ijcai
source ijcai/bin/activate

# 3. 安装所有依赖
pip install -r requirements.txt

# 4. 验证安装
python3 -c "from src.agent import SemanticFINDER; print('✓ 安装成功')"
```

---

## ⚡ 最快安装方式

如果只需要运行训练（不使用 LLM），最小安装：

```bash
pip install torch-geometric gymnasium scipy
```

---

**总结**: 当前项目缺少 **3 个必需依赖包**（torch-geometric, gymnasium, scipy），安装后即可运行。

---

**最后更新**: 2024-01-09

