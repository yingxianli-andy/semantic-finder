# 项目结构说明

**整理日期**: 2024-01-09  
**项目位置**: `/root/autodl-tmp/ijcai`

---

## 📁 当前项目结构

```
ijcai/
├── README.md                    # 项目说明
├── requirements.txt             # 依赖包列表
├── config/                      # 配置文件目录
│   └── config.yaml             # 主配置文件
├── src/                         # 源代码目录
│   ├── __init__.py
│   ├── agent/                   # 安迪：RL Agent 相关代码
│   │   ├── __init__.py
│   │   ├── semantic_finder.py  # Semantic-FINDER 主类
│   │   ├── encoder.py          # GraphSAGE/GCN 编码器
│   │   ├── decoder.py          # DQN 解码器
│   │   ├── replay_buffer.py    # 经验回放缓冲区
│   │   └── reward.py           # 奖励函数定义
│   ├── environment/             # 哈工爷：环境模拟器
│   │   ├── __init__.py
│   │   ├── opinion_env.py      # OpinionDynamicsEnv 主类
│   │   ├── dynamics.py         # DeGroot/Friedkin-Johnsen 模型
│   │   └── data_loader.py      # 数据加载器
│   ├── llm/                     # 上交爷：LLM 接口
│   │   ├── __init__.py
│   │   ├── controller.py       # LLM 控制器主类
│   │   ├── prompt_template.py  # Prompt 模板
│   │   └── semantic_feature.py # 语义特征提取
│   ├── visualization/           # 上交爷：可视化
│   │   ├── __init__.py
│   │   ├── gephi_exporter.py   # Gephi 导出工具
│   │   └── color_mapper.py     # 颜色映射工具
│   ├── utils/                   # 工具函数
│   │   ├── __init__.py
│   │   ├── graph_utils.py      # 图处理工具
│   │   └── metrics.py          # 评估指标
│   └── test/                    # 测试代码
├── experiments/                 # 实验脚本
│   ├── train.py                # 训练脚本
│   ├── test.py                 # 测试脚本
│   └── ...
├── data/                        # 数据目录
│   ├── raw/                     # 原始数据（SNAP 数据集）
│   ├── processed/               # 处理后的数据
│   └── synthetic/               # 合成图数据
├── results/                     # 实验结果
│   ├── models/                 # 保存的模型
│   ├── logs/                   # 训练日志
│   ├── figures/                # 生成的图表
│   └── test/                   # 测试结果
├── notebooks/                   # Jupyter notebooks
├── scripts/                     # 工具脚本
│   └── download_datasets.py   # 数据集下载脚本
└── doc/                         # 文档目录
    ├── 开发文档.md
    ├── 接口对接文档.md
    ├── 程序说明文档.md
    └── ...
```

---

## ✅ 文件完整性检查

### 核心代码模块

- ✅ `src/agent/` - RL Agent（安迪）
  - semantic_finder.py
  - encoder.py
  - decoder.py
  - replay_buffer.py
  - reward.py

- ✅ `src/environment/` - 环境模拟器（哈工爷）
  - opinion_env.py
  - dynamics.py
  - data_loader.py

- ✅ `src/llm/` - LLM 接口（上交爷）
  - controller.py
  - prompt_template.py
  - semantic_feature.py

- ✅ `src/visualization/` - 可视化（上交爷）
  - gephi_exporter.py
  - color_mapper.py

### 实验脚本

- ✅ `experiments/train.py` - 训练脚本
- ✅ `experiments/test.py` - 测试脚本

### 配置文件

- ✅ `config/config.yaml` - 主配置文件
- ✅ `requirements.txt` - 依赖包列表
- ✅ `README.md` - 项目说明

### 目录结构

- ✅ `data/` - 数据目录（已创建）
- ✅ `results/` - 结果目录（已创建）
- ✅ `notebooks/` - Notebooks 目录（已创建）

---

## 🚀 下一步操作

### 1. 创建虚拟环境

```bash
cd /root/autodl-tmp/ijcai
python3 -m venv ijcai
source ijcai/bin/activate
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 验证安装

```bash
# 测试导入
python3 -c "from src.agent import SemanticFINDER; print('✓ Agent 模块正常')"
python3 -c "from src.environment import OpinionDynamicsEnv; print('✓ Environment 模块正常')"
python3 -c "from src.llm import LLMController; print('✓ LLM 模块正常')"
```

### 4. 开始使用

```bash
# 训练
python experiments/train.py --num_episodes 100 --budget 10 --n_nodes 50 --device cuda

# 测试
python experiments/test.py --model_path results/models/final_model.pth --dataset_name ego-Twitter
```

---

## 📊 项目状态

- ✅ **代码完整性**: 100%
- ✅ **目录结构**: 已整理
- ✅ **配置文件**: 已就位
- ⚠️ **依赖安装**: 待安装
- ⚠️ **数据准备**: 待下载（可选）

---

**项目已整理完成！** 🎉

