# Semantic-FINDER 核心代码打包说明

**打包日期**: 2024-01-08  
**压缩包名称**: `semantic-finder-core.tar.gz`  
**压缩包大小**: **2.1 MB**

---

## 📦 包含内容

### 核心代码
- ✅ `src/` - 所有源代码
  - `src/agent/` - RL Agent（安迪）
  - `src/environment/` - 环境模拟器（哈工爷）
  - `src/llm/` - LLM 接口（上交爷）
  - `src/visualization/` - 可视化工具（上交爷）
  - `src/utils/` - 工具函数
  - `src/test/` - 测试代码

### 实验脚本
- ✅ `experiments/` - 训练和测试脚本
  - `train.py` - 训练脚本
  - `test.py` - 测试脚本

### 配置文件
- ✅ `config/` - 配置文件目录
  - `config.yaml` - 主配置文件

### 文档
- ✅ `doc/` - 所有文档
  - 开发文档
  - 接口对接文档
  - 程序说明文档
  - API 文档
  - 等等

### 工具脚本
- ✅ `scripts/` - 工具脚本
  - `download_datasets.py` - 数据集下载脚本

### 项目文件
- ✅ `requirements.txt` - 依赖包列表
- ✅ `README.md` - 项目说明

---

## ❌ 不包含内容

- ❌ `data/` - 数据目录（需要单独下载）
- ❌ `results/` - 实验结果目录
- ❌ `ijcai/` - 虚拟环境目录
- ❌ `__pycache__/` - Python 缓存
- ❌ `*.pyc` - 编译的 Python 文件
- ❌ `.git/` - Git 仓库（如果有）

---

## 🚀 在新服务器上使用

### 1. 解压

```bash
tar -xzf semantic-finder-core.tar.gz
cd semantic-finder-core  # 或解压后的目录名
```

### 2. 创建虚拟环境

```bash
python3 -m venv ijcai
source ijcai/bin/activate  # Linux/Mac
# 或
ijcai\Scripts\activate  # Windows
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 准备数据（可选）

```bash
# 下载数据集
python scripts/download_datasets.py --dataset ego-Twitter

# 或手动下载后放到 data/raw/ 目录
```

### 5. 开始使用

```bash
# 训练
python experiments/train.py --num_episodes 100 --budget 10 --n_nodes 50 --device cuda

# 测试
python experiments/test.py --model_path results/models/final_model.pth --dataset_name ego-Twitter
```

---

## 📊 文件统计

- **总文件数**: 约 50+ 个文件
- **代码文件**: Python 源文件
- **文档文件**: Markdown 文档
- **配置文件**: YAML 配置文件

---

## ✅ 完整性检查

打包前已确认包含：
- ✅ 所有源代码模块
- ✅ 训练和测试脚本
- ✅ 配置文件
- ✅ 完整文档
- ✅ 依赖列表

---

**打包完成！** 🎉

