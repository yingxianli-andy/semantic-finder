# 运行前检查清单

**检查日期**: 2024-01-09  
**项目位置**: `/root/autodl-tmp/ijcai`

---

## ❌ 当前缺失项

### 1. 虚拟环境（可选但推荐）

**状态**: ❌ 未创建

**创建方法**:
```bash
cd /root/autodl-tmp/ijcai
python3 -m venv ijcai
source ijcai/bin/activate
```

**说明**: 虽然系统已安装部分包，但建议使用虚拟环境隔离依赖。

---

### 2. 缺失的依赖包（必需）

#### ❌ torch-geometric
**状态**: 未安装  
**影响**: Agent 模块无法导入  
**安装**:
```bash
pip install torch-geometric
# 或使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple torch-geometric
```

#### ❌ gymnasium
**状态**: 未安装  
**影响**: Environment 模块无法导入  
**安装**:
```bash
pip install gymnasium
```

#### ⚠️ transformers（可选）
**状态**: 未安装  
**影响**: LLM 功能无法使用（测试时需要）  
**安装**:
```bash
pip install transformers accelerate
```

---

### 3. 代码问题

#### ⚠️ environment/__init__.py
**状态**: 需要检查导出  
**问题**: `OpinionDynamicsEnv` 可能未正确导出

---

## ✅ 已安装的依赖

- ✅ torch (2.8.0+cu128)
- ✅ networkx
- ✅ numpy
- ✅ scipy
- ✅ tqdm
- ✅ pyyaml

---

## 🚀 快速安装步骤

### 方法 1: 使用 requirements.txt（推荐）

```bash
cd /root/autodl-tmp/ijcai

# 创建虚拟环境（可选）
python3 -m venv ijcai
source ijcai/bin/activate

# 安装所有依赖
pip install -r requirements.txt

# 如果网络慢，使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

### 方法 2: 手动安装缺失的包

```bash
# 必需包
pip install torch-geometric gymnasium

# 可选包（LLM 功能）
pip install transformers accelerate
```

---

## ✅ 安装后验证

```bash
# 测试导入
python3 -c "from src.agent import SemanticFINDER; print('✓ Agent 正常')"
python3 -c "from src.environment import OpinionDynamicsEnv; print('✓ Environment 正常')"
python3 -c "from src.llm import LLMController; print('✓ LLM 正常')"
```

---

## 📋 完整检查清单

### 代码文件
- [x] ✅ 所有源代码文件存在
- [x] ✅ 实验脚本存在
- [x] ✅ 配置文件存在

### 依赖包
- [x] ✅ torch (已安装)
- [x] ✅ networkx (已安装)
- [x] ✅ numpy, scipy (已安装)
- [ ] ❌ **torch-geometric (缺失)**
- [ ] ❌ **gymnasium (缺失)**
- [ ] ⚠️ transformers (可选，LLM 需要)

### 环境
- [ ] ⚠️ 虚拟环境 (可选，推荐创建)

### 数据
- [x] ✅ 数据目录已创建
- [ ] ⚠️ 真实数据集 (可选，训练时不需要)

---

## 🎯 最小运行要求

**要运行训练，至少需要**:
1. ✅ torch (已有)
2. ❌ **torch-geometric** (需要安装)
3. ❌ **gymnasium** (需要安装)
4. ✅ networkx, numpy, scipy (已有)

**要运行测试（使用 LLM），还需要**:
5. ⚠️ transformers (可选)

---

## 📝 安装命令（一键安装）

```bash
cd /root/autodl-tmp/ijcai

# 创建虚拟环境
python3 -m venv ijcai
source ijcai/bin/activate

# 安装依赖（使用国内镜像加速）
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    torch-geometric \
    gymnasium \
    transformers \
    accelerate \
    tqdm \
    pyyaml

# 验证安装
python3 -c "from src.agent import SemanticFINDER; print('✓ 安装成功')"
```

---

**最后更新**: 2024-01-09

